// Dyscalculia shared constants (Phase-1 SAFE: no behavioral change)

// Dashboard weak-area threshold
export const WEAK_AREA_ACCURACY_THRESHOLD_PERCENT = 70;
export const WEAK_AREA_MIN_ATTEMPTS = 5;

// Balloon game feedback timings
export const BALLOON_RETRY_DELAY_MS = {
  reducedMotion: 1400,
  default: 2000,
};

// Balloon game particle settings (kept for consistency if used by UI)
export const BALLOON_CONFETTI_PIECES = {
  reducedMotion: 20,
  default: 50,
};

// General layout sizes
export const FLOATING_STARS_COUNT = 50;

